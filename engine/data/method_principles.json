{
  "_node": "method_principles",
  "_generated_by": "hand-authored DATA source (render-from-data); the method-side companion to library principles.html",
  "_render_target": "engine/data/method_principles.json (verbatim mirror) → a method-principles.html page, parallel to the library's principles.html",
  "_authority": "the cross-cutting method concepts that are NOT per-node. Per-output method lives in each node's drawer (§2.16). This page holds what applies across all nodes.",
  "version": 3,
  "date": "2026-08-11",
  "page": {
    "title": "The Method: principles and reusable components",
    "subtitle": "how the whole instrument works, and the pieces it reuses everywhere",
    "reads": "This is the method-side companion to the per-node drawers. The drawers tell you what each step IS; this page tells you the concepts and components that run across all of them."
  },
  "engine_development_loop": {
    "note": "THE GOAL of engine work + the loop that reaches it. Read this FIRST — it is the thing SBs most often lose sight of (measuring the engine instead of improving it).",
    "goal": "For EACH of the 5 Situate aspects (Sitz · Paradigm · Transmission · Lineage · Form): a GOOD version of the coding engine — one that PASSES its checks — written as a LIVE, re-runnable script, and CODED on the 3 golden-path texts (MARK-16-WEB · MARK-16-GRC · TTC-CH1-EN). The deliverable is the improved ENGINE itself, not a set of findings about it.",
    "the_loop": [
      "1. RUN the test on the text (vs-Wild, and/or a 3DR crit) — get concrete feedback on where the engine is weak.",
      "2. MAKE THE ENGINE CHANGE — edit the coder prompt/fields to fix what the test found. THIS is the step that gets skipped.",
      "3. RE-RUN the engine on the text.",
      "4. ITERATE steps 1-3 until it PASSES on the text.",
      "5. THEN code the improved engine on the other 2 texts."
    ],
    "the_anti_pattern": "Measure-and-handoff: running tests + writing findings / CLCs / 3DRs, then handing off WITHOUT making the engine change. A test exists to IMPROVE the engine, not to be filed. If a session ran a test but did not change an engine, it did not advance the goal.",
    "all_five_need_live_engines": "Every aspect needs a LIVE, re-runnable coder script so the loop can run. Live: Sitz=engine0_ground_sitz_paradigm.py · Paradigm=engine05_detect_paradigm.py · Lineage=engine_lineage_detector_s157.py · Form=code_form_subengine_s168.py. NOT yet an engine: TRANSMISSION is seed-loaded (load_transmission_seed_s161.py) and must be built into a live detector.",
    "pass_criterion": "vs_wild PASS is earned by an IN-SESSION multi-model convergence run (3 coders, e.g. opus/sonnet/haiku) — the way Paradigm passed — NOT by an external panel. An external 3DR is a CRIT that sharpens the engine, not the gate. AND (3DR-20): the honest PASS test is on an UNDERSTUDIED text — a naive read of a heavily-commented passage (Mark 16) is a compression of centuries of commentary, so coverage-parity there is uninformative.",
    "what_passing_means": "The engine wins on DISCIPLINE, not coverage: reproducible per-anchor structure, scope/verse-range discipline, directionality (sources vs reception, not later parallels), negative findings a naive read omits, cross-engine consistency, and uniform depth over ALL moves — never the claim that it \"sees more than a naive reader.\""
  },
  "reusable_components": {
    "note": "The method is built from a small set of reusable components (templates). Each is applied at many nodes, not just one. Name them once here; each node's drawer points back.",
    "items": [
      {
        "name": "The four field-shapes",
        "is": "Every coded field is one of four shapes. Knowing the shape tells a coder how to fill it and a reader how to trust it.",
        "shapes": [
          {
            "shape": "Closed value-list (pick from it)",
            "mechanic": "You must choose an allowed value; the list grows only by a locked extension (fit-or-extend).",
            "examples": "Frame's axes + their values (from the atlas reader); paradigm.stance {endorsed/invoked/subverted/denied}; lineage.borrowing_function"
          },
          {
            "shape": "Closed type (pick ONE)",
            "mechanic": "Assign the item to one category from a fixed set.",
            "examples": "the gloss's types (choose one)"
          },
          {
            "shape": "Probe set (ask ALL)",
            "mechanic": "A fixed battery of questions or lenses run over every item, not a value you pick.",
            "examples": "the gap loci (ask these of every gap); the 8-axis frame read (score every axis)"
          },
          {
            "shape": "Open field + coverage scan (scan families, name freely, and beyond)",
            "mechanic": "The name is open and emergent (anti-pre-anchoring); a checklist of kinds only reminds you to look widely, never limits the names.",
            "examples": "paradigm (scan narrative/ritual/political/literary/geographic/social, then name what you find); the move operation's TYPE (an open label per move)"
          }
        ],
        "lifecycle": "A field's shape is not permanent. An OPEN field can graduate to CLOSED once breadth stabilizes its values, through a lock cycle (CLC), the same way the Frame axes started open and became a locked value-list (the atlas precedent). When it graduates, lock only the FAMILY level and keep the specifier open (the move's TYPE is open now, ~40 values on one text; after running the instrument across traditions we lock its families and keep the specifier free). VOICE, by contrast, is closed from the start because grammar bounds it (active / middle-passive / null-stative will not sprout new values across traditions). The rule: lock a field only after breadth shows the set has stopped growing; locking from one text imports that text's hypothesis (anti-pre-anchoring).",
        "reuse": "When a new field is added to any engine, classify it as one of these four before coding it; and record whether it is closed-from-the-start (grammar-bounded) or open-now-lockable-later (awaiting breadth)."
      },
      {
        "name": "The grain rule",
        "is": "A field is coded at the passage grain, the move grain, or both, wherever both carry signal.",
        "mechanic": "Frame/Sitz/Paradigm code at BOTH passage and move; Lineage is move-native (passage = the roll-up); Witness is text-native (transmission distance is not per-move).",
        "reuse": "For any new coded field, decide its grain(s) before running it."
      },
      {
        "name": "The drawer template",
        "is": "Every node's drawer uses the same sections in the same order, so a reader always knows where to look.",
        "sections": [
          "1. Header: title, subtitle, the test pills (Cold Read, vs-Wild), the Fidelity stamp, and an honest status line",
          "2. What it is (one paragraph)",
          "3. Why it matters (one paragraph)",
          "4. How it works: what you give it, what the method does, what you get back",
          "5. The method (clean, text-agnostic): the step's grammar and the definition of every piece of it",
          "6. The method applied: the same grammar run on the golden-path text (the worked example, with its concrete values)",
          "7. Where the work is saved: the store, the home row, and the key (findability)",
          "8. Honest limits",
          "9. Made by: the scripts that produce the step and this card's data",
          "10. What's next: the flow to the next node",
          "11. Foundation: what this node's method leans on (the established frameworks + prior work it stands on), stated openly; the full decision-trail (how we arrived, the CRTs, the IRRs) lives in the research view",
          "12. Research view (collapsible): the receipt, how to run it, the data source, and the decision-trail"
        ],
        "reuse": "Build every new drawer to this order; a reader learns it once and finds everything by position. Sections 5 and 6 hold the clean-then-applied split; section 11 (Foundation) records what the method reuses / leans on, so the method visibly stands on established scholarship rather than reinventing it."
      },
      {
        "name": "The Cold Read",
        "is": "The check that a card (or a coding call) is self-sufficient: give it to a fresh model with no other context and see if it acts correctly.",
        "mechanic": "Seven questions: orient (what/where/next) + reproduce (can you run this step) + gap-scan (what's confusing/missing) + duplication-and-tier check (MECE across black=main / grey=footnote / hidden=researcher view). Run as a small cohort; fix to convergence. Include a decoy.",
        "reuse": "Run on every drawer card before it is called done, and as a TIE-BREAKER to settle contested coding cells (a resolvable adjudication)."
      },
      {
        "name": "The instrument-vs-wild test",
        "is": "The check that our fields/definitions actually help an LLM code the text, versus a naive LLM with no scaffolding.",
        "mechanic": "Run the same passage two ways: instrument arm (our fields) vs wild arm (just 'what are the X?'), across several models. The primary metric is RELIABILITY: does the instrument make coding reproducible where the wild arm can't be aggregated? Breadth is the second axis (does it see everything the wild arm sees).",
        "reuse": "Run per coded aspect; the result sets the aspect's second pill (PASS = beats wild on reliability + breadth; PARTIAL = reliability but a gap; FAIL = wild >= instrument)."
      },
      {
        "name": "The closed/open field contract",
        "is": "Every field declares whether it is closed (controlled vocabulary), open (free text), or numeric, and names its vocab source.",
        "mechanic": "Closed vocabularies render LIVE from their store (Frame from exom_axis_vocabulary), never hardcoded, so they cannot go stale. Where a closed enum has no formal vocab table yet, it renders the distinct coded values + flags 'controlled-by-convention, formal vocab owed.'",
        "reuse": "Applied to every field in every aspect; pairs with the four field-shapes."
      },
      {
        "name": "The move-grammar",
        "is": "The unit the whole method works on. A move is one agent doing one operation to one thing, with one result.",
        "mechanic": "Start a new move when the agent, operation, or result changes; when in doubt keep the move whole (under-split loses less than over-split). Moves connect (one feeds the next; one reports another's speech).",
        "reuse": "Every downstream step reads moves: gaps hang off them, glosses answer their questions, comparisons line them up."
      },
      {
        "name": "The strengthening loop",
        "is": "How the instrument improves. The tests find where a field is weak, we strengthen it, we re-run the tests, and the pill moves.",
        "mechanic": "The wild test (and the Cold Read gap-scan) expose a gap. A tie-breaker (a small convergence panel of fresh models) settles the contested cells; a 3DR crit surfaces what is missing and what to sharpen. We strengthen the fields into a versioned upgrade, re-code the worked example, and re-run the instrument-vs-wild A/B. If the gap closes and reliability holds, the second pill moves from PARTIAL to PASS. The instrument demonstrably improves under adversarial testing, which is itself part of the argument.",
        "reuse": "Run per aspect. Paradigm is the first worked example (v1 to the 3DR-11 crit plus the tie-breaker to v2); then Sitz, Witness, Lineage."
      },
      {
        "name": "The handle and its definition",
        "is": "The unit for every coded field: a handle (the term) plus a 1-2 sentence operational description that DECIDES what the thing is and lets a second coder apply it, not a gesture. The Frame axis values are the model, e.g. 'abduction: infers the best explanation, reasoning back from a surprising fact to what would, if true, make it a matter of course.'",
        "mechanic": "Closed-vocabulary fields draw the definition from the vocab store (Frame from exom_axis_vocabulary.definition), shared across every use. Open fields (the paradigm name, the sitz) get the description from the CODER, AT CODING TIME, per instance. The definition is operational in the Lotze sense: it says what the thing is and lets you apply it, never an aesthetic image and never a restatement of the handle.",
        "reuse": "Every coded field, everywhere. Produced at coding time by the coder, never back-filled by an editor or an LLM at the end. If it is not coded in the instrument, we do not use it later: the reading room and the essays are built from the coded fields, so the definition must exist as part of the code, or it does not exist."
      },
      {
        "name": "The controlled-value contract",
        "is": "How a closed-vocabulary field behaves and how it shows in the drawer. Three parts: closed-first, multi-where-it-co-occurs, candidate-when-nothing-fits.",
        "mechanic": "CLOSED-FIRST: the coder picks from the controlled list. MULTI: a field may allow more than one value when the phenomenon is genuinely plural (a move acts ON X, TO Y, WITH Z at once; a claim can be testimony AND tradition AND revelation), like the atlas reader's primary + secondary; SINGLE otherwise. CANDIDATE, at TWO levels: (1) a new VALUE within a field, if nothing in the list fits, routed to a review queue (fit-or-extend), never force-fitting a wrong one; (2) a new MODIFIER AXIS itself, at a MUCH higher bar  -  the default is always to fit the existing set; a coder proposes a new axis only when a phenomenon genuinely fits NONE of the existing modifiers, exactly as the atlas reader keeps its 8 axes and resists a 9th unless it is absolutely necessary and cannot fit. Same discipline already proven in the atlas reader (primary/secondary + a resisted 9th axis) and the catalogue vocab/candidate tables.",
        "reuse": "Every closed field, in every drawer, declares and SHOWS five things in the method (clean) section, render-from-data: { handle | definition | vocab (the allowed values) | single-or-multi | candidate-allowed }. So a coder reads not just what a field is, but its values, whether they may pick more than one, and whether they may propose a new one. Refines the four field-shapes (makes the closed shape precise); pairs with the handle-and-its-definition."
      }
    ]
  },
  "principles": {
    "note": "The load-bearing commitments the whole method runs on.",
    "items": [
      {
        "name": "Coded at the instrument, or not used",
        "principle": "Everything the reading room and the essays draw on is captured AT CODING TIME, by the coder, as a handle plus an operational description. Nothing is back-filled by an editor or an LLM at the end. If a field is not coded in the instrument, it does not exist, and we do not use it later.",
        "why": "It is what keeps the instrument reproducible. The appearance of rigor applied after the fact (the 'magic hand') is exactly what the method forbids: a description added later is one editor's polish, not the coder's judgment, and the fidelity and instrument-vs-wild checks cannot grade a field that is not there."
      },
      {
        "name": "Two visualization registers",
        "principle": "The project has two goals, so it has two kinds of visualization, chosen on purpose. ANALYTICAL: for the science (the instrument is the subject, the tests are the data); aggregate, comparative, precise. COMPREHENSION: for the reading (a single text's meaning, dwelt in, a lectio-divina and Talmud register); focused, evocative, single-instance.",
        "why": "The two want opposite design instincts (dense and neutral versus spare and warm). Building one with the other's instinct fails: a contemplative reading room that looks like a dashboard, or a coverage chart too pretty to read precisely. Name the register before designing the view."
      },
      {
        "name": "One record, every output (the pinch-point)",
        "principle": "Decompose and Situate write into ONE assembled record per passage (the enriched move), and every downstream output reads only that record: the close read, the docent summary, the gaps, the gloss, compare, resituate, and the essays. It is the pinch-point of the bow-tie: everything on the left funnels into it, everything on the right derives from it.",
        "why": "Get its structure right and every output inherits the rigor; get it wrong and every output inherits the error. That is why the record's schema matters more than any single drawer or view, and why it is worth building carefully before the outputs multiply."
      },
      {
        "name": "The record is stable, the presentation is disposable (headless)",
        "principle": "The enriched move lives as a defined table with fixed columns, decoupled from any surface that displays it. The reading room is one head; an essay or a model is another. Redesign the reading room's look and feel as often as you like: the surface reads from the table and renders however it wants, and the data never moves when the design does.",
        "why": "It is a headless (decoupled, CMS-style) content store for the reading. The same neutral-feed rule as the checkout: never marry the data to a surface. It is what lets the look and feel evolve for years without a single re-code, because insertion is stable regardless of how the room is redesigned."
      },
      {
        "name": "Read the method clean, then the method applied",
        "principle": "Each drawer separates two zones and never conflates them. First the METHOD, stated clean and text-agnostic: what the step does and the definition of every piece of its grammar, with no worked-example values mixed in. Then the METHOD APPLIED: the same grammar run on the golden-path text (the worked example), where the concrete values and counts live. A definition is stated once, as a legend; the worked example shows only labeled values, never re-printing the definitions.",
        "why": "It keeps the reusable instrument legible on its own (you can read the grammar without the example, and reuse it on any text) and keeps the demonstration honest (the example applies the method, it does not silently redefine it). Mixing them is how invented terms and text-specific assumptions creep into what should be a portable method."
      },
      {
        "name": "Fix the engine, not the output (fix once, fix sustainably)",
        "principle": "When a check (Cold Read, Fidelity, vs-Wild) or any review finds a defect, fix the ENGINE or GENERATOR that produced it - the code that re-runs - never the single output. A wrong rendered value is fixed in the generator so every regeneration is correct; a wrong coded value is fixed in the extractor (the coding engine); a value that should be constrained is constrained at ONE source that all consumers import. Never hand-edit the output to make a check pass. Corollary - SINGLE SOURCE: when the same fact lives in two places (e.g. a closed vocabulary in the extractor AND the drawer), collapse it to one definitional home that coding, rendering, and checking all import, with a drift-check that fails loud if the coded data ever exceeds it - so two copies cannot silently diverge.",
        "why": "A hand-patched output is a one-off that recurs the next time the engine runs, and it turns a pill green while the instrument still produces the defect - the worst outcome, because it hides the problem behind a false pass. Fixing the engine makes the fix durable, keeps the check honest, and lets every future run inherit it. Worked example (S162): the Cold Read found the drawer hid legal-but-unused vocab; the fix was not to type the missing values into the JSON but to add a single-source CLOSED_VOCAB that the generator renders from, the extractor codes from, and the fidelity check drift-guards - engine fixed, not output patched."
      },
      {
        "name": "The unsaid is a positive datum · three grains",
        "principle": "Negative space is CODED, not detected after the fact — and it runs over the whole engine as THREE grains, each a different step. (1) LIBRARY — what the corpus never acquired: upstream, an acquisition question, LACUNA; visible from the catalogue alone. (2) CODED-ELEMENT — absence WITHIN a coded text, recorded DURING coding as a first-class coordinate (Position: not-engaged / not-asserted / apophatic; Connect: missing-edge; Situate aspects: unspecified / withheld). (3) ANALYSIS — discovery and closure over the recorded coordinates, downstream, by ONE generalized gap-analyzer. Build rule: RECORD-IN / ANALYZE-OUT — every component records a shared negative-space coordinate during coding; one downstream analyzer reads it. No gap-finder per component; nothing runs as an upfront pre-filter except LACUNA. The seam: 'deliberate silence' is defined once — apophatic (frame) = BARRED (grammar) = deliberate-missing-edge (connect) — referenced everywhere, computed nowhere twice.",
        "why": "Same discipline as 'coded at the instrument': a silence is a claim only if it is recorded as a coordinate at coding time, not inferred by a later reader. Two honesty rules ride on it. The clean grain-boundary: if a silence can only be seen by READING a text we hold, it is grain 2/3 (the gap engine), never LACUNA — that is what stops the corpus-level and text-level 'unsaid' from being computed twice. The reliability split: coders agree a silence is PRESENT (alpha≈0.59) but not that it is OBLIGATED / deliberate (alpha≈0.03), so 'present' and 'obligated' are separate fields and the deliberate call is carried as PROVISIONAL, never locked as settled."
      }
    ]
  },
  "the_four_checks": {
    "note": "The four checks the method runs on a card or a coded field. Rule (naming-lock, S162): card-level checks are named as short WORDS, never letter-acronyms, so none collides with CRT, which is reserved for the higher-level method below.",
    "checks": [
      {
        "name": "Cold Read",
        "asks": "Can a fresh reader act correctly from this card alone?",
        "tests": "self-sufficiency",
        "on_card": "graded pill (PASS / PARTIAL)"
      },
      {
        "name": "Cold Start",
        "asks": "Can a fresh agent find where the prior work lives (its store, its key)?",
        "tests": "findability",
        "on_card": "gate / flag"
      },
      {
        "name": "Fidelity",
        "asks": "Do the card's labels equal the real source columns, with no invented terms?",
        "tests": "label truth",
        "on_card": "verified-stamp / gate"
      },
      {
        "name": "vs-Wild",
        "asks": "Does our scaffolding beat an unscaffolded LLM (primary metric: reliability)?",
        "tests": "instrument value",
        "on_card": "graded pill (PASS / PARTIAL / FAIL)",
        "scoping_rule": "LAYER-SCOPED (S162): the wild control must be prompted to do ONLY the job of the layer under test - never the whole reading. Testing DECOMPOSE, the wild arm lists what is SAID (the moves: who does what to what, with what result) and is forbidden from (a) the connections BETWEEN moves (that is Connect's job) and (b) the unsaid / withheld (that is the silence/LACUNA layer's job). An unscoped wild reader always 'beats' one layer because it uses all three - that is testing the wrong thing. Each layer gets its own scoped wild control; the FULL-STACK test (Decompose + Connect + LACUNA vs a full wild read) is the eventual gold standard, run once all layers exist.",
        "scoping_example": "Decompose vs-Wild, Mark 16 (2026-08-09): unscoped wild LOST-scoping made it look like the instrument lost, but the losses were exactly Connect (between-move irony) + LACUNA (negative space) - the next layers. Re-run scoped to 'just the moves': the wild reader's 22 moves all sit inside the instrument's 60, which add typed process_type/voice/polarity/force - a fair PASS for the Decompose layer."
      }
    ],
    "reserved": {
      "name": "CRT = Clean Room Triangulation",
      "is": "A higher-level method (Decision 200) that serves many purposes; NOT one of the card checks. The abbreviation CRT is reserved for it. The card self-sufficiency check is Cold Read, never CRT."
    }
  },
  "the_two_pills": {
    "note": "Two of the four checks render as pills on a coding card, so what we're proving is visible on the card. (Fidelity shows as a verified-stamp; Cold Start is a gate, not a pill.)",
    "cold_read_pill": "Cold Read: is the card self-sufficient (a fresh reader acts correctly from it alone)? States: PASS / PARTIAL.",
    "vs_wild_pill": "vs-Wild: does our scaffolding beat a naive LLM (primary metric: reliability)? States: PASS / PARTIAL / FAIL.",
    "text_exception": "Text (the checkout) is not a coding step, so it carries only the Cold Read pill."
  },
  "drawer_build_checklist": {
    "note": "The checklist for building or revising a drawer node, so nothing is dropped (tests, writing, subheadings, data structure). Review START before touching a node; FINISH before calling it done. Full detail with the why behind each item: pm/30_methodology/engine/CHECKLIST_drawer_build.",
    "phases": [
      {
        "phase": "START - orient before you build",
        "items": [
          "Read the SSOT engine page for this lane, then re-read THIS node's drawer, before the markdown handover.",
          "Pull the node's real data from the DB on the owner's key; do not trust the handover's claim of what is coded.",
          "Fix each field's shape (closed value-list / closed-type / probe-set / open) and grain (passage / move / both) before coding.",
          "Every coded field is a handle plus an operational definition, written at coding time; nothing back-filled at the end.",
          "Build to the drawer template: the eleven fixed sections, in order."
        ]
      },
      {
        "phase": "DURING - build from data, define the grammar",
        "items": [
          "Render from the generator to a data file; never hardcode or hand-edit the deployed json.",
          "Define the grammar clean and text-agnostic, then apply it in the worked example; state a definition once, as a legend.",
          "Fidelity: every label is a real source column or a declared alias; never invent a term (the Cold Read cannot catch an invented term).",
          "Plain but precise; term first with a plain gloss in parens; no double-say; no em-dashes."
        ]
      },
      {
        "phase": "FINISH - test, land, verify",
        "items": [
          "Run both checks: Fidelity (the gate) and Cold Read (self-sufficiency, with a decoy and a gap-scan); write the run doc.",
          "Check em-dashes on the file text, not on a serialized string (json.dumps hides them).",
          "Findability: the where-saved section names the store, the home row, and the key.",
          "Land it and verify LIVE: commit, push, hand design the data json, then fetch the live page and confirm the change is there.",
          "Tell design which branch or commit is authoritative; re-ping on each new push.",
          "Record any new name on this page first, then a decision; reserve CRT for Clean Room Triangulation."
        ]
      },
      {
        "phase": "STRENGTHEN - for a pinch-point node",
        "items": [
          "Run a 3DR completeness round on the node's grammar, reconcile, and CLC-walk the candidates (LOCK / EXTEND / DROP / DEFER).",
          "Validate by running the node out of domain, not by more debate; use is the decisive test."
        ]
      }
    ]
  },
  "glossary": {
    "note": "The method's terms, with a disambiguating line where a word means something narrower than everyday usage. (To populate fully; seeds below.)",
    "terms": [
      {
        "term": "move",
        "def": "one agent doing one operation to one thing, with one result. The unit the method codes"
      },
      {
        "term": "Sitz",
        "def": "the world the text came out of; the conditions that conditioned it (short for Sitz im Leben, extended cross-tradition)"
      },
      {
        "term": "Paradigm",
        "def": "a cultural template or script the text runs on (narrative, ritual, political, literary, social). The name is open and emergent"
      },
      {
        "term": "Frame",
        "def": "the text's position on the 8-axis interpretive placement (epistemic-warrant, ontological-commitment, inferential-operation, telos, evaluative-stance, hermeneutic-posture, r_topology, r_condition)"
      },
      {
        "term": "Lineage",
        "def": "the earlier sources a move stands on (which move cites/echoes which source)"
      },
      {
        "term": "Witness",
        "def": "the text's transmission-distance from the origin event (degrees, composition year/place)"
      },
      {
        "term": "checkout",
        "def": "the library handing the method one verbatim, sourced, anchored passage to code"
      },
      {
        "term": "gap / silence",
        "def": "what a move leaves unsaid. The negative space the instrument is built to find"
      }
    ]
  },
  "supporting_material_roadmap": {
    "note": "What a cold reader most wants alongside the per-node drawers, ranked from a 3-model Cold Read Test (2026-08-08). Building order.",
    "items": [
      {
        "item": "End-to-end worked example (the golden path)",
        "status": "BUILDING",
        "why": "the #1 need, unanimous. The per-node drawers can't prove the pipeline coheres; one passage all the way through, showing each handoff, does. This is the Mark 16 golden path."
      },
      {
        "item": "A second worked example on a different tradition",
        "status": "PLANNED",
        "why": "named by 2/3. One example invites 'you overfit to Mark 16'; a non-scripture / other-tradition run answers it (matches the replication commitment)."
      },
      {
        "item": "This field-mechanics reference (the four shapes)",
        "status": "DONE (above)",
        "why": "the skeptic's first check: is each field a closed list, an open field, or a checklist?"
      },
      {
        "item": "Glossary of method terms",
        "status": "SEEDED (above)",
        "why": "disambiguate the in-house vocabulary (Frame/Witness mean something narrower than everyday usage)."
      },
      {
        "item": "Reliability / validation explainer",
        "status": "PLANNED",
        "why": "what the two pills certify; where reliability is high vs low, stated honestly; how disagreements are adjudicated (a tie-breaker Cold Read cohort)."
      },
      {
        "item": "Consolidated method FAQ / objections-and-answers",
        "status": "PLANNED (merge the two scattered rebuttal FAQs + the per-node ones)",
        "why": "pre-empt the committee: circularity, cherry-picking, trusting an LLM coder, 'a boundary is itself a claim,' reproducibility."
      },
      {
        "item": "How to read the bow-tie diagram",
        "status": "PLANNED",
        "why": "the visual grammar (the pinch point, required vs optional arrows) is load-bearing and non-obvious."
      },
      {
        "item": "Scope & limits + positioning vs existing methods",
        "status": "PLANNED",
        "why": "when NOT to use it; what's new vs form criticism / narrative criticism / discourse analysis, the committee's first question."
      },
      {
        "item": "Blank coding templates",
        "status": "PLANNED",
        "why": "the convinced reader still can't act without the empty forms."
      }
    ]
  },
  "reliability_explainer": {
    "note": "What the two pills certify, and where the method is solid vs still soft. Honesty is the point: an unvalidated coding scheme is just a vocabulary.",
    "crt_certifies": "That a card is self-sufficient: a fresh reader acts correctly from it alone (orient + reproduce + no confusion + MECE tiers).",
    "instrument_pill_certifies": "That our fields make coding more reproducible than a naive LLM (the reliability metric), and how broad the coverage is.",
    "where_reliable": "The SET of a passage's paradigms, the dominant, and the structured fields converge across independent coders. The move-boundary and the 8 frame axes are the most rigorously tested.",
    "where_soft": "Stance and fine-grained salience are the least reproducible; the dominant needs the manifest-salience rule (what the text foregrounds), not the deepest theological frame. Named honestly, not hidden.",
    "adjudication": "Disagreements are settled by a tie-breaker Cold Read cohort (a small cohort of fresh models), which also surfaces the definition fix that caused the split, e.g. the split that led to holding stance (own terms) / is_dominant (manifest salience) / relation_to_dominant (rival effect) strictly separate."
  },
  "reproducibility_test": {
    "note": "A reusable TEST-TYPE (not a 5th check): the measurement-method BENEATH Fidelity + vs-Wild's \"primary metric: reliability.\" It is the discipline that keeps catching single-run over-claims, so it is documented here as a template every engine runs the same way.",
    "name": "Independent-sample reproducibility (within-run Jaccard)",
    "what_it_certifies": "That the coder produces the SAME coded set across INDEPENDENT samples — reliability, not one good run. It feeds the Fidelity stamp (R1-R3) and is vs-Wild's primary metric.",
    "the_template": [
      "1. Run N independent samples of the coder on the SAME text — one FRESH conversation per sample (a subprocess each). NEVER repeated calls on one shared backend: that measures in-context echo, not reproducibility (the S169 confound — two identical N=5 runs gave Jaccard 0.30 vs 1.0).",
      "2. Compute mean pairwise Jaccard over the coded set. Measure it on CANONICAL handles, not raw free-text names — raw-name variation hides real agreement (Mark 16: raw 0.007 vs canonical 0.803).",
      "3. Report the number to field_reliability (measure=mean_pairwise_jaccard) + the count spread + the stable core."
    ],
    "the_load_bearing_warning": "REPRODUCIBLE != VALID. A consistently FORCED set is reproducibly wrong. A high Jaccard can be an artifact of pre-anchoring (a handle-menu the coder keeps matching) rather than genuine detection. So ALWAYS pair the reproducibility number with a VALIDITY check on an OUT-OF-DOMAIN text (does the coder mint text-appropriate codes, or force the home-text's codes onto a foreign one?). This test caught two over-claims in S170: the lineage directionality \"fix\" (one lucky run) and the paradigm reuse-before-mint reliability (0.973 on Mark 16, but the handles were DERIVED from Mark 16 — circular; the out-of-domain test is the honest arbiter).",
    "where_it_plugs_in": "Under Fidelity (the reliability stamp) and vs-Wild (reliability = the primary metric). Not a separate pill."
  },
  "faq": {
    "note": "Objections and answers. Consolidates the two scattered rebuttal FAQs (academic/00_thesis/METHOD_REBUTTALS_FAQ + pm/40_architecture/method_rebuttals_faq_REWORKED) + the per-node FAQs. Key ones below; full merge owed.",
    "items": [
      {
        "q": "Isn't move-segmentation as subjective as the interpretation it claims to formalize?",
        "a": "The move is a container-level unit (agent-operation-result); boundaries carry a confidence flag and err toward under-splitting. Segmentation is auditable, and its reliability is measured, not assumed."
      },
      {
        "q": "Does Situate smuggle in the conclusion (circularity)?",
        "a": "Situate attaches context per-field with declared inputs and gates; the bias is made visible in audit logs rather than hidden behind curatorial selection."
      },
      {
        "q": "You built this to fit Mark 16: does it generalize?",
        "a": "The golden path proves the pipeline coheres on one passage; a second end-to-end run on a non-Christian text tests it out of domain. Replication failures are findings, not embarrassments."
      },
      {
        "q": "Why trust an LLM coder?",
        "a": "The instrument-vs-wild test measures whether the fields make coding reproducible versus a naive LLM; reliability is graded on aggregate rates, and disagreements are adjudicated by a tie-breaker cohort."
      },
      {
        "q": "A boundary (a silence, a move edge) is itself an interpretive claim.",
        "a": "Yes, and it is flagged as a claim with provenance and confidence, not presented as a neutral given."
      },
      {
        "q": "Isn't this just close reading, or form criticism?",
        "a": "See positioning: the new part is reproducible multi-lens coding, the negative-space (gap/silence) focus, cross-tradition comparison, and AI-augmented inter-rater reliability."
      }
    ]
  },
  "positioning": {
    "note": "What is new versus adjacent methods: the committee's first question.",
    "vs": [
      {
        "method": "Form criticism (Sitz im Leben)",
        "new": "extends Sitz cross-tradition and adds the 10-category substrate, coded reproducibly rather than essayistically."
      },
      {
        "method": "Narrative / rhetorical criticism",
        "new": "formalizes 'moves' as a coded unit with a grammar, so readings are comparable across texts, not one-off."
      },
      {
        "method": "Discourse / move analysis",
        "new": "adds the situating layers (Frame, Paradigm, Lineage, Witness) and the negative-space (gap/silence) engine."
      },
      {
        "method": "Close reading",
        "new": "makes the reading reproducible (multi-lens, multi-coder, measured reliability) instead of resting on a single expert reader's authority."
      }
    ]
  },
  "open_questions": {
    "note": "Named honestly so they don't hide.",
    "items": [
      {
        "q": "Where does figuration (idioms, symbols, metaphor) live?",
        "state": "OPEN. Figuration is a conventional SIGN, distinct from paradigm (a SCRIPT). Candidate homes: gloss-level unpacking (now), or a dedicated figuration layer coded at the move level (later, via a CLC). Flag it to the coverage-scan, don't fold it into paradigm."
      }
    ]
  },
  "grammar_versioning": {
    "note": "How the move-grammar is versioned, so a run_tag (grammar-version x text) stays meaningful. The run_tag is the re-run detector: a text coded under an older MAJOR needs re-coding; a MINOR refinement does not.",
    "major": "vN (v2, v3, ...) - a STRUCTURAL change: a core part or modifier added/removed, the funnel changed, or a coded field's meaning changed incompatibly. Requires a NEW run_tag (move_grammar_vN_<date>) and a full RE-CODE of every text; the version x text detector flags the backlog.",
    "minor": "vN.M (v2.1, v2.2, ...) - a within-structure REFINEMENT: a definition clarified, a rendering fix, a coverage rule locked (e.g. operation_force = universal), an engine refactor (single-source wiring). Coding-COMPATIBLE - same fields, same values - so NO re-code is required and the run_tag stays move_grammar_vN_<date>. Logged in METHOD_CHANGELOG with the vN.M label.",
    "current": "v2.1 (2026-08-09): the v2 structure (Decision 223) + the S162 refinements - operation_force universal coverage, the full-closed-set vocab rendering, the Cold Read definition fixes, and the single-source engine wiring. run_tag remains move_grammar_v2_2026-08-09 (coding-compatible; no re-code).",
    "v3_is": "reserved for the next STRUCTURAL iteration (a genuinely new grammar), never for refinements."
  }
}