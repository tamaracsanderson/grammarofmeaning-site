{
  "what": "DWS maintained status — the human-knowledge layer (open threads, waiting-on-whom). The pages/sources map is auto-derived in dws_manifest.json; this file holds what a scanner can't know. Edit by hand when a thread opens/closes.",
  "last_updated": "2026-08-05",
  "role_one_liner": "design-SB is the RENDER layer: it owns the deploy repo (grammarofmeaning-site), renders every drawer/page from the other SBs' DATA exports, self-merges site PRs, and is read-only on twelve-laws.",
  "conventions": [
    "Render-from-data, never hardcode (§2.16). A page fetches a mirrored DATA artifact; if it can't render from data yet, that's a flagged gap, not a licence to hardcode.",
    "Mirror flow: owner-SB exports DATA on twelve-laws main → design-SB `git show origin/main:… > engine/data/X.json` → page fetches it → deploy (self-merged site PR).",
    "schema.html is the flagship (PI author-watches it): change it only via fail-safe monkey-patches in an isolated <script> block — main script byte-identical, typeof-guards + try/catch → fall back to the prior render. Never edit the 175KB const P blob by hand.",
    "Offline validation (no pane needed for static pages): regex-aware JS balance scan + replay the render logic against the real data in Python + curl the deployed file. Confirm zero bracket-delta vs known-good HEAD.",
    "Pane-gated = scroll/drag/step interaction that only exists live; can't be verified offline. Build those with a live browser (mine, or the PI as eyes)."
  ],
  "open_threads": [
    {"item": "Frame drawer + no-fork gospel morphospace", "status": "waiting", "on": "Librarian", "detail": "needs the corrected 8-axis atlas export (n=911 site JSON replacing the hardcoded MS_PTS ×15 + 3 exclusions + Frame-drawer export). No longer blocked on the MCA-builder question; queued as atlas-lane work after the PI's morning go-ahead."},
    {"item": "Abduction-run viz (2 panels: Concordia debate + forward-test lineup)", "status": "live", "on": "reading-SB + PI (eyeball)", "detail": "SHIPPED v1 at /reading-room-preview/abduction.html — offline-validated (JS balance + md-render replay + curl). Panel B forward-test bars render from abductive_forward_test.json (Lineage 0.85 best; debate-top r_topology 0.35 reproduces worst); Panel A the full verbatim deliberation renders from the readable md. EXPLORATORY frame + pilot-LLM-judge caveat surfaced; the removed 3-lever gate table does NOT appear. Awaiting reading-SB + PI live eyeball for the final accuracy check."},
    {"item": "Scroll-ribbon dual reading (the Talmudic per-gospel reading)", "status": "live", "on": "PI (scroll-test) + Reading/Method SB (verse-map)", "detail": "v1 SHIPPED at /reading-room-preview/reading.html — each gospel read as its coded MOVES in spine order + a sticky rail (frame ribbon lighting up veers + paradigm + sitz + lineage) that updates via IntersectionObserver as you scroll. PI scroll-testing the sync live. NEXT: Reading/Method SB (161) is building a move→verse aligner (re-exporting coded_move_state with verse_start/verse_end + verse_refs) — once it lands I render the actual WEB scripture verse under each move (the full Talmudic layout, verse-precise) and reading.html supersedes voices.html. Companion to moves.html: moves.html = analytical (heatmap), reading.html = reading-room."},
    {"item": "Rename schema.html → 'Method & Instrument' (parallel to 'The Library')", "status": "buildable-now", "on": "design-SB", "detail": "PI-approved: promote the noun name 'Method & Instrument' + demote 'what a text is made of' to tagline; link the library flywheel's OUTPUT ① 'the instrument' card → schema.html; update nav labels (library-schema currently 'the Method schema') + the method-schema alias. Coordinated pass; headers mapped. Paused mid-way to build the scroll-ribbon while the PI was at her computer; resume next."},
    {"item": "Resituation CLC dispositions", "status": "waiting", "on": "PI + reading-SB", "detail": "after the PI walks the 12 CLC candidates, reading-SB re-runs export_resituation_drawer_s160.py → design-SB re-mirrors resituation_drawer.json (pure data swap) → the drawer's next-steps flip pending→LOCK/EXTEND/DROP/DEFER automatically."}
  ]
}
